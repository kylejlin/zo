use super::*;

impl JuneConverter {
    pub(crate) fn convert_for(
        &mut self,
        expr: &jnode::For,
        context: Context,
    ) -> Result<znode::Expr, SemanticError> {
        todo!()
    }
}
