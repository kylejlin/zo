enum Nat
    case zero
    case succ(pred: Nat)

enum* Eq(T: Type, left: T) ^(right: T)
    case refl ^(left)

def add(dec a: Nat, b: Nat) -> Nat
    match a
    case zero:
        b
    case succ(a_pred):
        succ(add(a_pred, b))
    end
    